# Dockerfile for the Streamlit UI
FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit requests

COPY runtime/streamlit_ui/ /app/

EXPOSE 8501

CMD ["streamlit", "run", "app.py", "--server.headless=true", "--server.address=0.0.0.0"]
